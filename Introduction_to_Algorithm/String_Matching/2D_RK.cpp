#include <iostream>
#include <cmath>
#include <vector>

using namespace std;
int main(){
    int n, m;
    cin >> n >> m;
    vector<string> T(n, "");
    vector<string> P(m, "");

    for (int i=0; i<n; i++) {
        cin >> T[i];
    }
    for (int i=0; i<m; i++) {
        cin >> P[i];
    }

    //todo
}